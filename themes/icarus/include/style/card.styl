/* ---------------------------------
 *              Card
 * --------------------------------- */
 @import "custom"
.card
    overflow: visible
    border-radius: $card-radius

    & + .card, & + .column-right-shadow
        margin-top: 1.5rem

    .card-image
        overflow: hidden
        border-radius: $card-radius
        border-radius: $card-radius

    .media + .media
        border: none
        margin-top: 0
